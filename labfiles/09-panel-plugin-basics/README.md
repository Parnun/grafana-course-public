# 09 Panel Plugin Basics

Lab นี้เตรียมแนวทางสำหรับใช้ `@grafana/create-plugin` ใน GitHub Codespaces

ค่าที่แนะนำสำหรับ prompt:

- Organization: `student`
- Plugin name: `codespace-panel`
- Plugin type: `panel`

คำสั่งหลักหลัง scaffold:

```bash
npm install
npm run dev
docker compose up
```