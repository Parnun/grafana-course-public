services:
  grafana:
    image: grafana/grafana:13.0.1
    container_name: grafana-lab-setup
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - grafana_setup_data:/var/lib/grafana

volumes:
  grafana_setup_data: {}